# ── PostgreSQL connection ──────────────────────────────────────────
PGHOST=127.0.0.1
PGPORT=3309
PGUSER=postgres
PGPASSWORD=your-password-here
PGDATABASE=tapsite
# Set to "true" if PG requires SSL
# PGSSL=true

# ── Tapsite API (for workspace management tools) ──────────────────
# URL of the tapsite server (for create_workspace, check_subdomain, update_workspace_domain)
TAPSITE_BASE_URL=http://localhost:5555
# API key from Profile → API Keys (format tsk_...)
TAPSITE_API_KEY=tsk_your_key_here

# Max rows returned per query (default 500)
MCP_DB_MAX_ROWS=500

# ── Server deployment (SSE mode) ──────────────────────────────────
# Set MCP_PORT > 0 to run as HTTP/SSE server instead of stdio.
# MCP_PORT=3100

# Shared secret for SSE endpoint auth.
# Clients must send Authorization: Bearer <token> or ?api_key=<token>.
# Leave empty to disable auth (NOT recommended for production).
# MCP_AUTH_TOKEN=your-secret-token-here
